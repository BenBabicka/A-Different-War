using System.Collections;
using System.Collections.Generic;
using UnityEngine;


[System.Serializable]
public class PlayerData {

   /* public float health;
    public float[] position;
    public float[] navPosition;
    public string name;
    public string weaponName;

    public PlayerData(PlayerSave player)
    {
        health = player.GetComponent<Health>().health;
        position = new float[3];
        position[0] = player.transform.position.x;
        position[1] = player.transform.position.y;
        position[2] = player.transform.position.z;
        if (player.GetComponent<UnityEngine.AI.NavMeshAgent>().hasPath)
        {
            navPosition = new float[3];
            navPosition[0] = player.GetComponent<UnityEngine.AI.NavMeshAgent>().pathEndPosition.x;
            navPosition[1] = player.GetComponent<UnityEngine.AI.NavMeshAgent>().pathEndPosition.y;
            navPosition[2] = player.GetComponent<UnityEngine.AI.NavMeshAgent>().pathEndPosition.z;
        }
        name = player.name;
        if (player.weapon)
        {
            weaponName = player.weapon.name;
        }
    }*/
}
